"""
patterns.py — Recurring friction detection.

Future role:
  - Scan loaded log entries for repeated categories, keywords, or time-of-day clusters
  - Score and rank friction patterns by frequency and recency
  - Return a structured summary of detected patterns for use in insights.py
"""


def detect_patterns(events: list) -> list:
    """Placeholder: will analyse a list of events and return detected patterns."""
    return []
